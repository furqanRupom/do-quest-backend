export enum TaskStatus {
    pending = 'PENDING',
    accepted = 'ACCEPTED',
    rejected = 'REJECTED',
    withdrawn = 'WITHDRAWN',
}

export enum PaymentStatus {
    active = 'ACTIVE',
    completed = 'COMPLETED',
    cancelled = 'CANCELLED',
}
